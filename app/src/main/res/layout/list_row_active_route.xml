<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/current_recording_layout"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginHorizontal="16dp"
    android:layout_marginTop="@dimen/md3_card_vertical_margin"
    android:layout_marginBottom="@dimen/md3_card_vertical_margin"
    android:clickable="true"
    android:focusable="true"
    app:cardBackgroundColor="@color/md_primaryContainer"
    app:cardCornerRadius="@dimen/md3_card_corner_radius"
    app:cardElevation="0dp">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="16dp">

    <TextView android:id="@+id/current_rec_header"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        android:layout_marginTop="8dp"
        android:textAlignment="viewStart"
        android:layout_marginBottom="2dp"
        android:textAppearance="@style/TextAppearance.Material3.LabelLarge"
        android:textColor="@color/md_onPrimaryContainer"
        android:text="@string/recording_in_progress"/>

    <TextView
        android:id="@+id/route_title_tv"
        android:textAppearance="@style/TextAppearance.Material3.TitleMedium"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text=""
        android:minLines="1"
        android:maxLines="2"
        android:singleLine="false"
        android:textAlignment="viewStart"
        android:textColor="@color/md_onPrimaryContainer"
        android:layout_marginTop="4dp"
        app:layout_constraintBottom_toTopOf="@id/recording_actions"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toBottomOf="@id/current_rec_header"
        android:ellipsize="end"/>

    <LinearLayout
        android:id="@+id/recording_actions"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:gravity="end|center_vertical"
        android:orientation="horizontal"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/route_title_tv">

        <com.google.android.material.button.MaterialButton
            android:id="@+id/playpause_btn"
            android:layout_width="wrap_content"
            android:layout_height="40dp"
            android:layout_marginEnd="8dp"
            android:gravity="center"
            android:maxLines="1"
            android:minHeight="0dp"
            android:minWidth="76dp"
            android:paddingHorizontal="16dp"
            android:text="@string/pause_recording"
            android:textAllCaps="false"
            android:textColor="@color/md_onErrorContainer"
            app:backgroundTint="@color/md_errorContainer"
            app:cornerRadius="16dp"
            app:icon="@drawable/ic_pause"
            app:iconGravity="textStart"
            app:iconPadding="6dp"
            app:iconSize="18dp"
            app:iconTint="@color/md_onErrorContainer" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/stop_btn"
            android:layout_width="wrap_content"
            android:layout_height="40dp"
            android:gravity="center"
            android:maxLines="1"
            android:minHeight="0dp"
            android:minWidth="76dp"
            android:paddingHorizontal="16dp"
            android:text="@string/stop_recording"
            android:textAllCaps="false"
            android:textColor="@color/md_onPrimary"
            app:backgroundTint="@color/md_primary"
            app:cornerRadius="16dp"
            app:icon="@drawable/ic_stop_recording"
            app:iconGravity="textStart"
            app:iconPadding="6dp"
            app:iconSize="18dp"
            app:iconTint="@color/md_onPrimary" />
    </LinearLayout>

    </androidx.constraintlayout.widget.ConstraintLayout>
</com.google.android.material.card.MaterialCardView>
